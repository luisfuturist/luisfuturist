import options from "lfds-tokens";
const { color } = options;

let colors = {
    decisions: {
        badge: {
            normal: color.gray['2'],
            primary: color.azure['3'],
        },
    },
};

export { colors };