import options from "lfds-tokens";
const { color } = options;

let decisions = {
    badge: {
        color: {
            normal: color.gray['2'],
            primary: color.azure['3'],
        },
    },
};

export default decisions;